services:
  web:
    build: .
    ports:
      - "127.0.0.1:5000:5000"
    volumes:
      - ./db:/app/db
    environment:
      - FLASK_ENV=development
      - FLASK_SECRET_KEY=mi-clave-segura-aleatoria-123
